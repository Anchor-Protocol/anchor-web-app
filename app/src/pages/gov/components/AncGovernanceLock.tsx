import { UIElementProps } from '@libs/ui';
import React from 'react';
import styled from 'styled-components';

function AncGovernanceLockBase(props: UIElementProps) {
  return <div>Lock</div>;
}

export const AncGovernanceLock = styled(AncGovernanceLockBase)``;
