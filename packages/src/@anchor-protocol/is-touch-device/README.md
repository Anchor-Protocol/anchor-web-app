# `@anchor-protocol/is-touch-device`

# Install

```sh
npm install @anchor-protocol/is-touch-device
```

# Usage

```js
import { isTouchDevice } from '@anchor-protocol/is-touch-device';

if (isTouchDevice()) {
  // DO SOMETHING
}
```
