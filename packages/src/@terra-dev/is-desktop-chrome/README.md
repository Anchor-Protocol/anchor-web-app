# `@terra-dev/is-desktop-chrome`

Check is currently browser the desktop chrome.

# Usage

```js
import { isDesktopChrome } from '@terra-dev/is-desktop-chrome';

if (isDesktopChrome()) {
  // DO SOMETHING
}
```
